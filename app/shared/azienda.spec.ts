/* tslint:disable:no-unused-variable */

import { addProviders, async, inject } from '@angular/core/testing';
import {Azienda} from './azienda';

describe('Azienda', () => {
  it('should create an instance', () => {
    expect(new Azienda()).toBeTruthy();
  });
});
